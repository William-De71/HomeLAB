# ── Artefacts générés par les scripts d'installation ──────────────────────
# Les scripts sont la source de vérité ; les fichiers ci-dessous sont produits
# à l'installation et propres à chaque machine. À déclarer par service.
GladysAssistant/docker-compose.yml
GladysAssistant/config.mk

# Le dossier _template n'est pas un service installable, mais un essai depuis
# ce dossier ne doit pas versionner d'artefact par mégarde.
_template/docker-compose.yml
_template/config.mk

# Surcharges locales de compose
docker-compose.override.yml

# ── Données persistantes des conteneurs ───────────────────────────────────
volumes/
data/
db_data/
logs/
*.log

# ── Fichiers d'environnement (secrets) ────────────────────────────────────
.env
.env.*
!.env.example

# ── Fichiers système et éditeurs ──────────────────────────────────────────
.DS_Store
Thumbs.db
*~
*.swp
*.swo
.idea/
.vscode/

# ── Outils locaux ─────────────────────────────────────────────────────────
# Données de session Claude Code (permissions locales, historique)
.claude/
